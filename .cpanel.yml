---
# Cleanup after deploy 1: remove the temporary public download of the backup and the
# leftover probe repositories. The backup itself stays in /home8/sp123519/site-backups.
deployment:
  tasks:
    - /bin/rm -f /home8/sp123519/public_html/backup-f888bd0467760ae7a68d839f.tar.gz
    - /bin/rm -rf /home8/sp123519/repositories/deploy-probe
    - /bin/rm -rf /home8/sp123519/repositories/deploy-probe-2
